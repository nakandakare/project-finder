- Bugs:

- Tasks:

- Memo: 
Cuando hace dispatch,  the wrapper component's callback is notified (connect) then run mapStateToProps, si hay diferencia con el state anterior, se re-renderiza sino no.

projectId ownerId requestUserId note        active (middleware auth)
44          41         45         "hola"     true

if accept request : 
change to inactive
projectId requestUserId in user_project_table
and notificate

if reject :
change to inactive
nothing
and notificate

Socket esta compartido entre notification.component y project-apply.component


Task completed:
cant post projects after register (notice that user id is sent as undefined to backend api "/addProject") 
Testear si notifySocket.on('projectRequest') funciona con varios request a la vez (por useEffect con []) ?
Lo mismo con notifySocket.on('projectRequest') ?